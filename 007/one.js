import { name, age, addAge, getAge } from './two.js';
// const profile = require('./two');

console.log(name);
console.log(age);
addAge();
addAge();
console.log(age); // 이번에는 출력이 되었죠?
console.log(getAge());